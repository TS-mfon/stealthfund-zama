import Link from "next/link";
import { ArrowUpRight, EyeOff, PlusCircle } from "lucide-react";
import { PageHeader, StealthShell } from "@/components/stealth/shell";
import { featuredRaises } from "@/lib/raises";

export default function Page() {
  return <StealthShell><PageHeader eyebrow="LIVE OPPORTUNITIES" title="Confidential raises" copy="Browse Sepolia campaigns. Funding totals and individual commitments remain encrypted."/><section className="market"><div className="sectionhead"><div><small>START HERE</small><h2>Pick a thesis. Fund privately.</h2><p>New users should claim faucet cUSD first, then join a raise.</p></div><Link className="pillLink" href="/faucet">Claim faucet</Link></div><div className="cards raiseCards">{featuredRaises.map((raise, index) => <article className={index === 0 ? "selected" : ""} key={raise.name}><div className={`visual ${raise.color}`}><span>{String(index + 1).padStart(2, "0")}</span><EyeOff/></div><div className="content"><small>{raise.tag}</small><h3>{raise.name}</h3><p>{raise.thesis}</p><div className="terms"><span><b>{raise.stage}</b> Stage</span><span><b>FHE</b> Commitments</span><span><b>{raise.deadline}</b> Deadline</span></div><Link href={raise.href}>Review and commit <ArrowUpRight size={16}/></Link></div></article>)}<article className="createCard"><div className="visual dark"><span><PlusCircle/></span><EyeOff/></div><div className="content"><small>FOUNDER</small><h3>CREATE YOUR OWN</h3><p>Pitch your protocol, set the encrypted threshold, and deploy a campaign from the factory.</p><Link href="/raises/create">Start a raise <ArrowUpRight size={16}/></Link></div></article></div></section></StealthShell>;
}
