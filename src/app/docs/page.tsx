import Link from "next/link";
import { PageHeader, StealthShell } from "@/components/stealth/shell";
import { stealthContracts } from "@/lib/stealth-config";

export default function Page() {
  return <StealthShell><PageHeader eyebrow="DOCUMENTATION" title="How to use StealthFund" copy="Founder and investor instructions for the Sepolia Zama build."/><section className="docs"><h2>What StealthFund is</h2><p>StealthFund is a confidential capital formation interface. It lets users fund campaigns with encrypted cUSD commitments. Wallets and transactions remain public because this is Ethereum Sepolia, but commitment amounts and private balances are encrypted with Zama FHE.</p><h2>Investor flow</h2><ol><li>Open <Link href="/faucet">/faucet</Link> and mint exactly 1,000 test cUSD.</li><li>Open <Link href="/raises">/raises</Link> and choose a campaign thesis.</li><li>Connect an Ethereum Sepolia wallet.</li><li>Enter a commitment amount or use a quick amount button.</li><li>The app validates campaign bytecode/state, approves the campaign as cUSD operator, encrypts through Zama, and submits the encrypted handle/proof.</li><li>After confirmation, the global portfolio records the participation transaction so the user can see it in <Link href="/portfolio">/portfolio</Link> from any browser.</li></ol><h2>Founder flow</h2><ol><li>Open <Link href="/raises/create">/raises/create</Link>.</li><li>Pitch what the protocol does and why it deserves funding now.</li><li>Set a future deadline and encrypted cUSD threshold.</li><li>Submit the factory transaction and open the returned campaign address.</li></ol><h2>Balance and portfolio display</h2><p>cUSD balances and commitment amounts are confidential on-chain. The UI does not mirror or fake plaintext balances. Portfolio storage keeps only confirmed participation metadata such as campaign address, transaction hash, timestamp, and status.</p><h2>Storage model</h2><p>StealthFund does not use a SQL database. Portfolio participation records are stored as Vercel Blob JSON objects by wallet address. Private values remain encrypted on-chain and are not persisted as plaintext in the app.</p><h2>Privacy model</h2><ul><li>Public: campaign contract, founder wallet, investor wallet, transaction timing, campaign state.</li><li>Encrypted: commitment amount, aggregate committed total, private position values, vote weights.</li><li>No public amount committed field is shown because that would defeat the product.</li></ul><h2>Contracts</h2><pre>{JSON.stringify(stealthContracts, null, 2)}</pre><h2>Troubleshooting</h2><ul><li>Start with faucet if commit fails for lack of cUSD.</li><li>Invalid address/checksum: use a route generated by the app; addresses are normalized before viem/Zama calls.</li><li>Faucet cooldown: the UI preflights cooldown before asking for a wallet signature.</li><li>Campaign not active: use an active campaign or ask the founder to activate it.</li><li>Encryption failure: confirm Sepolia, wallet connection, RPC availability, and Zama relayer access.</li></ul></section></StealthShell>;
}
